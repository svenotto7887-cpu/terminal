{
  "name": "terminal",
  "version": "0.1.0",
  "description": "Professional trading terminal built on SEI Chain",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=frontend\" \"npm run dev --workspace=backend\"",
    "build": "npm run build --workspace=frontend && npm run build --workspace=backend",
    "test": "npm run test --workspace=frontend && npm run test --workspace=backend",
    "lint": "npm run lint --workspace=frontend && npm run lint --workspace=backend",
    "clean": "npm run clean --workspace=frontend && npm run clean --workspace=backend && rm -rf node_modules",
    "type-check": "npm run type-check --workspace=frontend && npm run type-check --workspace=backend"
  },
  "keywords": [
    "trading",
    "terminal",
    "sei",
    "blockchain",
    "crypto"
  ],
  "author": "Terminal Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
